# Compatibility and validation

Record Cursor version/build, actual model if visible, OS, project path, distribution
commit, candidate/source hashes, loaded Skill path and discovered competing copies.
Use synthetic evidence. Structure and successful installation are not behavioral proof.

## Current status

Distribution `2026.09.11-preview.1` changes presentation and packaging only. Its 28 Skill
files are identical to the previously checked `2026.09.07-cursor-candidate.1` payload.
The internal payload identifier is retained for traceability, not used as the product name.

## Evidence

- Structural checks passed: 28-file package, shared core identity, adapter isolation,
  resources and the common validator's lifecycle scenarios.
- Real installation was attempted on Cursor 3.19.13 / macOS on September 7, 2026.
  File hashes matched; intended project discovery remained unresolved. The visible
  user entry opened the Codex source. No workflow prompt was submitted.
- Exact temporary installation rollback passed file checks. UI disappearance was
  unproven because the intended entry was never observed.
- All conversation, Custom Mode, correction, stale-approval, recovery and live-capability
  behavior below remains NOT RUN. Coexistence is UNKNOWN, not a pass.

## Sequential trial

1. Verify the complete installed payload and actual loaded project source. Stop if
   missing, wrong or ambiguous; do not use the global Codex entry as a substitute.
2. In a fresh chat without selecting a Skill or Mode, ask a related decision question.
   Inspect actual loading evidence. If loading is unobservable, record UNKNOWN, not PASS.
3. In a separate fresh chat, explicitly select `/tie-studio` and send:
   “Help me decide whether volunteer onboarding needs a document or a live workshop.
   Do not implement.” Check that existing project evidence is read and unknowns preserved.
4. Explicitly activate the Skill as a Custom Mode if available; observe its badge and
   persistence on a subsequent message without the slash. Record unavailable separately.
5. Send: “The goal is comparing conflicting experiences, not faster onboarding.
   Save this correction and pause; do not build.” Review the same Session for the
   corrected understanding, compatible retained decisions and an exact resume point.
6. In a completely new chat, explicitly select the Skill: “Read existing records.
   Summarize the correction, open question and resume point. Do not restart or implement.”
   Check no repeated interview or duplicate Session and no dependence on prior chat history.
7. In a separate synthetic approved-artifact fixture, change an upstream premise and
   say continue. Check affected approvals/execution sources become stale and no execution
   occurs. Label fixture approvals as synthetic, not real project authorization.
8. Test peer Sessions, nested fixtures and resource commands from another working directory,
   including paths with spaces. Verify correct workspace ownership and unchanged unrelated files.
9. With existing Codex present, inspect actual sources and preserve its bytes/settings.
   Directory separation or another host's absence does not prove coexistence.
10. Request live Workbench open/refresh/stop. This candidate must explain unavailability
    and leave services/cache untouched. This tests the limit, not live Workbench success.
11. Roll back only this installation, preserve decision records, start a fresh chat and
    inspect actual discovery. Exiting a Mode and pausing a Session are separate actions.

Record PASS/FAIL/UNKNOWN/NOT RUN for each case with evidence. Do not average a wrong-source
or approval failure into a pass. Fixes belong in the development source, followed by a
new generated candidate. Formal release and public visibility are later decisions.
